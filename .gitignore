node_modules/
.env
.DS_Store
*.log

# state/ НЕ игнорируется намеренно: слепки прода лежат в git, чтобы обе машины
# видели одну картину, а история изменений между деплоями получалась сама собой
# через git diff — отдельный детектор для этого не нужен.
